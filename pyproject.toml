[project]
name = "airbnb-snowflake-dbt"
version = "0.1.0"
description = "Airbnb dbt and Snowflake project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.6",
    "dbt-snowflake>=1.11.2",
]
